<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.ProgrammingMapper">

	<resultMap id="programming" type="egovframework.example.sample.service.ProgrammingVO">
		<result property="pno" column="pno"/>
		<result property="writer" column="writer"/>
		<result property="subject" column="subject"/>
		<result property="contents" column="contents"/>
		<result property="nowdate" column="nowdate"/>
	</resultMap>

	<insert id="insertProgramming" parameterType="ProgrammingVO">

			INSERT INTO PROGRAMMING
				(  PNO
				  , WRITER
				  , SUBJECT
				  , CONTENTS 
				  , NOWDATE )
			VALUES ( #{pno}
				  , #{writer}
				  , #{subject}
				  , #{contents}
				  , #{nowdate} )

	</insert>

	<update id="updateProgramming">

			UPDATE PROGRAMMING
			SET PNO=#{pno}
				, WRITER=#{writer}
				, SUBJECT=#{description}
				, CONTENTS=#{useYn}
				, NOWDATE=#{nowdate}
				  WHERE PNO=#{pno}

	</update>

	<delete id="deleteProgramming">

			DELETE FROM PROGRAMMING
			WHERE PNO=#{pno}

	</delete>

	<select id="selectProgramming" resultMap="programming">

			SELECT
				PNO, WRITER, SUBJECT, CONTENTS, NOWDATE
			FROM PROGRAMMING
			WHERE PNO=#{pno}

	</select>

	<select id="selectProgrammingList" parameterType="ProgrammingVO" resultType="egovMap">

			SELECT
				PNO, WRITER, SUBJECT, CONTENTS, NOWDATE
			FROM PROGRAMMING
			ORDER BY PNO ASC
	</select>

</mapper>